<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Yuzhen.eRestaurant.repository.before.UserRepository">
	<select id="isUse" parameterType="BUser" resultType="BUser">
		select * from busertable where desknum = #{desknum}
	</select>
	<select id="login" parameterType="BUser" resultType="BUser">
		select * from busertable where desknum = #{desknum}
	</select>
</mapper>